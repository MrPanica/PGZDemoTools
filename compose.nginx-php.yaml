services:
  app:
    image: demotools-pgz-demo-tools:latest
    command: ["python", "demo_tools.py", "serve", "--host", "0.0.0.0", "--port", "8765", "--no-browser"]
    expose:
      - "8765"

  nginx:
    image: nginx:alpine
    ports:
      - "8771:80"
    volumes:
      - ./nginx/default.conf:/etc/nginx/conf.d/default.conf:ro
    depends_on:
      - app

  php:
    image: php:8.5-fpm-alpine
